# my pi agent config

Pi agent configuration and customizations.

## Directory Structure

- `extensions/` - Custom pi extensions
- `settings.json` - Pi agent settings

## Adding New Extensions

Place custom extension `.ts` files in the `extensions/` directory. Pi will automatically load them on startup.

