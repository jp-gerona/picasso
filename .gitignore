# Secrets - keep local only
auth.json
**/auth.json
.env
.env.*

# Pi runtime state (created when this repo is used as ~/.pi/agent)
.pi/
sessions/
git/
mcp-cache.json
mcp-npx-cache.json
run-history.jsonl
models-store.json
bin/
session-manager-config.toml
pi-crash.log

# Dependencies / generated environments
node_modules/
**/node_modules/
.venv/
**/.venv/

# Python junk
__pycache__/
**/__pycache__/
*.pyc

# OS/editor junk
.DS_Store
Thumbs.db
tmp/
*.log


# Agent-produced research output - fetched content, repo-review notes,
# future feature output. Local only; never ship in the config repo.
# repo-review's curated checklist.md lives at skills/repo-review/ and
# ships tracked, outside this drawer.
references/
